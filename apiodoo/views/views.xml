<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="apiodoo.list">
      <field name="name">apiodoo list</field>
      <field name="model">apiodoo.apiodoo</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="apiodoo.action_window">
      <field name="name">apiodoo window</field>
      <field name="res_model">apiodoo.apiodoo</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="apiodoo.action_server">
      <field name="name">apiodoo server</field>
      <field name="model_id" ref="model_apiodoo_apiodoo"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="apiodoo" id="apiodoo.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="apiodoo.menu_1" parent="apiodoo.menu_root"/>
    <menuitem name="Menu 2" id="apiodoo.menu_2" parent="apiodoo.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="apiodoo.menu_1_list" parent="apiodoo.menu_1"
              action="apiodoo.action_window"/>
    <menuitem name="Server to list" id="apiodoo" parent="apiodoo.menu_2"
              action="apiodoo.action_server"/>
    -->
  </data>
</odoo>